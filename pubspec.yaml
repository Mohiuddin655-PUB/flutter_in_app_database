name: in_app_database
description: "Efficient in-app database for seamless data storage and retrieval, ensuring optimal performance and user experience."
version: 1.2.5
repository: https://github.com/Mohiuddin655-PUB/flutter_in_app_database.git

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  in_app_query: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.2

flutter:
